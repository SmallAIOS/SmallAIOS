\* TLC Model Checker Configuration for I2S DMA Ring Buffer

CONSTANTS
    BufSize = 4

SPECIFICATION Spec

INVARIANTS
    TypeInvariant
\* WriteOnlyToFree: write_ptr can wrap to non-FREE slot when buffer is full (by design)
\*   Write safety is enforced by BeginWrite guard (requires FREE slot)
    ReadOnlyFromReady
    PointersInRange
    DMACoherency
    SlotAccountingInvariant
    ReadNeverExceedsWrite

\* EventualProgress is a liveness property; omit under bounded checking
