[workspace]
resolver = "2"
members = [
    "kernel",
    "arch/x86_64",
    "arch/aarch64",
    "arch/riscv64",
    "arch/nvidia",
    "arch/intel_gpu",
    "onnx-rt",
    "ipc",
    "net",
    "posix",
    "security",
    "container",
    "bus",
    "bench",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
